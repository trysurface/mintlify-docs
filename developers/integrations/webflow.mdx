---
title: "Webflow"
description: "Add a Surface Form to your Webflow Site"
icon: "webflow"
og:title: "Add a Surface Form to your Webflow Site"
twitter:title: "Add a Surface Form to your Webflow Site"
og:description: "Add a Surface Form to your Webflow Site"
twitter:description: "Add a Surface Form to your Webflow Site"
og:image: "https://blog.withsurface.com/api/og?title=Webflow%20Integration"
twitter:image: "https://blog.withsurface.com/api/og?title=Webflow%20Integration"
---

<Tip>This feature requires being on a paid Webflow site plan.</Tip>

<img src="/images/webflow/webflow_empty_code.png" style={{ borderRadius: '0.5rem' }} />

<Warning>
  Before proceeding, make sure you have installed the Surface Tag on your website. Follow these steps to install the Surface Tag first:
  <Steps>
    <Step>
      Go to the webflow [*dashboard*](https://webflow.com/dashboard).
    </Step>
    <Step>
      Go to `Project Section` > `Custom Code`.
    </Step>
    <Step>
      Add the following code in the *Start of `<head>` tag* section.
        
    ```html
    <!-- Start Surface Tag -->
    <script src="https://cdn.jsdelivr.net/gh/trysurface/scripts@latest/surface_tag.min.js"></script>
    <!-- End Surface Tag -->
    ```

    The `head` section containing the `Surface Tag` should now look like this:
    <img src="/images/webflow/webflow_head_section_tag.png" style={{ borderRadius: '0.5rem' }} />
    </Step>
  </Steps>
</Warning>

### Embedding a Surface Form in your Webflow Site

There are three different ways to embed a Surface Form.

<Tabs>
    <Tab title="Inline Embed">
        To embed an **inline form**, first go to the Webflow [dashboard](https://webflow.com/dashboard) > `Project Section` > Select `Custom Code` and add the following custom code into the `footer` code section.
        
        ```html
        <!-- Start Surface Form Embed -->
        <script>
            window.onload = function() {
                // Replace this with your Surface Form URL
                const surface_src = "<form url here>"
                
                const surface_embed_type = "inline"
                const target_element_class = "surface-form-target"
                const c = new SurfaceEmbed(surface_src, surface_embed_type, target_element_class)

                c.embedSurfaceForm();
            }
        </script>
        <!-- End Surface Form Embed -->
        ```

        Here's what the Webflow `footer` code section should look like:
        <img src="/images/webflow/webflow_inline_footer_code.png" style={{ borderRadius: '0.5rem' }} />

        Next, navigate to your Webflow Design Studio and select the section where the form will be added.
        After selecting the section, you should see the `Style` tab in the right panel.
        <img className="block" src="/images/webflow/webflow-design-studio.png" alt="webflow" />
        
        Now add `surface-form-target` in the Style Selector:
        <img className="block" src="/images/webflow/webflow-design-studio-inline.png" alt="webflow" />
    </Tab>
    <Tab title="Popup">
        To configure a **popup form**, first go to the Webflow [dashboard](https://webflow.com/dashboard) > `Project Section` > Select `Custom Code` and add the following custom code into the `footer` code section.
        
        ```html
        <!-- Start Surface Form Embed -->
        <script>
            window.onload = function() {
                // Replace this with your Surface Form URL
                const surface_src = "<form url here>"

                const surface_embed_type = "popup"
                const target_element_class = "surface-form-button"
                const c = new SurfaceEmbed(surface_src, surface_embed_type, target_element_class)
            
                // Optional: Set the popup size ("small", "medium", or "large")
                c.popupSize = "medium"
            }
        </script>
        <!-- End Surface Form Embed -->
        ```

        Here's what the Webflow `footer` code section should look like:
        <img src="/images/webflow/webflow_popup_footer_code.png" style={{ borderRadius: '0.5rem' }} />

        Next, navigate to your Webflow Design Studio and select the button that triggers the form.
        After Selecting the button, you should see `Style` tab on your right panel.
        <img className="block" src="/images/webflow/webflow-design-studio.png" alt="webflow" />
        
        Finally, add `surface-form-button` in Style Selector:
        <img className="block" src="/images/webflow/webflow-design-studio-popslide.png" alt="webflow" />
    </Tab>
    <Tab title="Slideover">
        To configure a **slideover form**, first go to the Webflow [dashboard](https://webflow.com/dashboard) > `Project Section` > Select `Custom Code` and add the following custom code into the `footer` code section.

        ```html
        <!-- Start Surface Form Embed -->
        <script>
            window.onload = function() {
                // Replace this with your Surface Form URL
                const surface_src = "<form url here>"

                const surface_embed_type = "slideover"
                const target_element_class = "surface-form-button"
                const c = new SurfaceEmbed(surface_src, surface_embed_type, target_element_class)
            }
        </script>
        <!-- End Surface Form Embed -->
        ```

        Here's what the Webflow `footer` code section should look like:
        <img src="/images/webflow/webflow_slideover_footer_code.png" style={{ borderRadius: '0.5rem' }} />

        Next, navigate to your Webflow Design Studio and select the button that triggers the form.
        After Selecting the button, you should see `Style` tab on your right panel.
        <img className="block" src="/images/webflow/webflow-design-studio.png" alt="webflow" />
        
        Finally add `surface-form-button` in Style Selector:
        <img className="block" src="/images/webflow/webflow-design-studio-popslide.png" alt="webflow" />
    </Tab>

</Tabs>

Finally, save and publish your changes. Your Surface Form should now be embedded on your Webflow website!
