---
title: "Step 2.2.2: Slideover"
description: "Embed a Surface Form slideover"
og:title: "Next.js Slideover | Surface Docs"
twitter:title: "Next.js Slideover | Surface Docs"
og:description: "Embed a Surface Form slideover"
twitter:description: "Embed a Surface Form slideover"
og:image: "https://app.withsurface.com/api/v1/form/og?title=Next.js%20Slideover"
twitter:image: "https://app.withsurface.com/api/v1/form/og?title=Next.js%20Slideover"
---

Let's add the **`SurfaceFormScript()`** component that we created in [**Step 2**](/guides/embedding/next/app-router/constructor) to the **`app/layout.tsx`** file:

```tsx
import Script from "next/script";
import SurfaceFormScript from "./surfaceFormScript";

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
return (
	  <html lang="en">
	  <head>
        <Script
          src="https://cdn.jsdelivr.net/gh/trysurface/scripts@latest/surface_tag.min.js"
          data-site-id="SITE_ID" // Replace this with your Site ID
          strategy="beforeInteractive"
         />
      </head>
		<body>
		 {children}
		  <SurfaceFormScript
           formUrl="REPLACE ME WITH FORM URL" // Replace this with a Surface Form URL
           embedType="slideover"
           buttonClassName="surface-form-button"
         />
		  </body>
	  </html>
	);
}
```

Add the `surface-form-button` className to the element you want to use to trigger the form.

```tsx
export default function Home() {
    return (
        <div>
            <button className="surface-form-button">Open Form</button>
        </div>
    );
}
``` 